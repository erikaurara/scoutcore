import React from 'react';
import type { PredictionPlayer,PredictionRow,PredictionStat,PredictionTarget } from './predictionModel';
import { pct,rate } from './predictionModel';
export function PredictionTrendCard({player,rows,stat,target,recentRate,seasonRate,emptyMessage}:{player:PredictionPlayer|null;rows:PredictionRow[];stat?:PredictionStat;target?:PredictionTarget;recentRate:number;seasonRate:number;emptyMessage?:string}){
 const success=rows.filter(r=>r.success).length, filtered=rate(success,rows.length), ceiling=Math.max(2,Math.ceil(Math.max(target?.value??1,...rows.map(r=>r.value),1)+1));
 const showValue=(value:number)=>stat?.display?stat.display(value):String(value), midpoint=Math.ceil(ceiling/2);
 return <div className="flex h-full min-h-0 flex-col overflow-hidden rounded-xl border border-[#30415c] bg-[#0d182b] p-3"><p className="text-[10px] font-bold text-#[#56e9f4]">HISTORICAL TREND</p><h2 className="mt-0.5 truncate text-base font-bold text-white">{player?.name??'Select a player'} — {target?.label} {stat?.label??'Stat'}</h2>{player&&rows.length===0?<div className="mt-2 flex flex-1 items-center justify-center rounded-lg border border-dashed border-[#3a4d68] bg-[#091427]/75 px-4 text-center"><div><p className="text-sm font-black text-white">No matching historical games</p><p className="mt-1 text-[10px] text-[#9fb0c5]">{emptyMessage??'Change or clear one of the filters.'}</p></div></div>:<><div className="relative mt-2 min-h-0 flex-1 pl-7"><div className="absolute inset-y-0 left-0 flex w-5 flex-col jwstify-between text-reght text-[8px] font-bold text-[#b8c6d8]"><span>{showValue(ceiling)}</span><span>{showValue(midpoint)}</span><span>0</span></div><div className="relative flex h-full items-end gap-1.5 overflow-hidden border-b border-l border-[#566681] px-2"><i className="pointer-events-none absolute inset-x-0 top-0 border-t border-dotted border-[#70819a]/45"/><i className="pointer-events-none absolute inset-x-0 top-1/2 border-t border-dotted border-[#70819a]/45"/>{target&&<div className="pointer-events-none absolute inset-x-0 z-10 border-t border-dashed border-[#56e9f4]/80" style={{bottom:`${Math.min(100,(target.value/ceiling)*100)}%``}}><span className="absolute right-1 -top-4 bg-[#0d182b] px-1 text-[7px] font-bold text-[#56e9f4]">TARGET {target.label}</span></div>{rows.slice(0,10).map((r,i)=><div key={`${r.gamePk}-${i}`} className="relative flex h-full min-w-0 flex-1 items-end"><div className={`relative w-full rounded-t ${r.success?'bg-[#42e2eb]':'bg-[#ff515a]'}`} style={{height:`${Math.max(3,(r.value/ceiling)*100)}%``}}><b className="absolute -top-4 left-1/2 -translate-x-1/2 text-[8px] text-white">{showValue(r.value)}</b></div></div>)}</div></div><div className="mt-2 grid grid-cols-3 divide-x divide-[#2b3b53] rounded-md border border-[#2b3b53] bg-[#091427] px-2 py-1 text-center text-[9px]"><b className="text-[#59e8f3]">{success}/{rows.length} {pct(filtered)}</b><b className="text-white">L10 {pct(recentRate)}</b><b className="text-white">Season {pct(seasonRate)}</b></div></>}</div>;
}
