import React from 'react';
import type { PredictionPlayer,PredictionRow,PredictionStat,PredictionTarget } from './predictionModel';
import { pct,rate } from './predictionModel';
export function PredictionTrendCard({player,rows,stat,target,recentRate,seasonRate}:{player:PredictionPlayer|null;rows:PredictionRow[];stat?:PredictionStat;target?:PredictionTarget;recentRate:number;seasonRate:number}){
 const success=rows.filter(r=>r.success).length, filtered=rate(success,rows.length), ceiling=Math.max(2,Math.ceil(Math.max(target?.value??1,...rows.map(r=>r.value),1)+1));
 const showValue=(value:number)=>stat?.display?stat.display(value):String(value);
 const midpoint=Math.ceil(ceiling/2);
 return <div className="rounded-xl border border-[#30415c] bg-[#0d182b] p-5"><p className="text-xs font-bold text-[#56e9f4]">HISTORICAL TREND</p><h2 className="mt-1 text-xl font-bold text-white">{player?.name??'Select a player'} — {target?.label} {stat?.label??'Stat'}</h2><div className="relative mt-5 h-[270px] pl-9"><div className="absolute inset-y-0 left-0 flex w-7 flex-col justify-between pb-1 text-right text-[10px] font-bold text-[#b8c6d8]"><span>{showValue(ceiling)}</span><span>{showValue(midpoint)}</span><span>0</span></div><div className="relative flex h-full items-end gap-2 overflow-x-auto border-b border-l border-[#566681] px-3"><i className="pointer-events-none absolute inset-x-0 top-0 border-t border-dotted border-[#70819a]/55"/><i className="pointer-events-none absolute inset-x-0 top-1/2 border-t border-dotted border-[#70819a]/55"/>{target&&<div className="pointer-events-none absolute inset-x-0 z-10 border-t-2 border-dashed border-[#56e9f4]/80" style={{bottom:`${Math.min(100,(target.value/ceiling)*100)}%`}}><span className="absolute right-1 -top-5 rounded bg-[#0d182b] px-1.5 text-[9px] font-bold text-[#56e9f4]">TARGET {target.label}</span></div>}{rows.map((r,i)=><div key={`${r.gamePk}-${i}`} className="relative flex h-full min-w-[42px] flex-1 items-end"><div className={`relative w-full rounded-t ${r.success?'bg-[#42e2eb]':'bg-[#ff515a]'}`} style={{height:`${Math.max(3,(r.value/ceiling)*100)}%`}}><b className="absolute -top-6 left-1/2 -translate-x-1/2 rounded bg-[#071225]/90 px-1.5 py-0.5 text-xs text-white shadow">{showValue(r.value)}</b></div></div>)}</div></div><div className="mt-4 grid grid-cols-3 divide-x divide-[#2b3b53] rounded-lg border border-[#2b3b53] bg-[#091427] p-3 text-center"><b className="text-[#59e8f3]">{success}/{rows.length} {pct(filtered)}</b><b className="text-white">L10 {pct(recentRate)}</b><b className="text-white">Season {pct(seasonRate)}</b></div></div>;
}
