import { useEffect,useMemo,useRef,useState } from 'react';
import { fetchTeams } from '../../services/mlbClient';
import type { PredictionPlayer,PredictionRow,PredictionSeasonMode,PredictionWindow } from './predictionModel';
import { clamp,HITTER_PREDICTION_STATS,PITCHER_PREDICTION_STATS,rate,succeeds } from './predictionModel';
import { addPredictionContext,clearPredictionFeedCache,fetchPredictionLogs } from './predictionData';

export function usePredictionWorkspace(){
 const [player,setPlayer]=useState<PredictionPlayer|null>(null),[teams,setTeams]=useState<any[]>([]),[logs,setLogs]=useState<any[]>([]),[rows,setRows]=useState<PredictionRow[]>([]);
 const [statId,setStatId]=useState('hits'),[targetIndex,setTargetIndex]=useState(0),[windowKey,setWindowKey]=useState<PredictionWindow>('L10'),[seasonMode,setSeasonMode]=useState<PredictionSeasonMode>('CURRENT'),[opponentId,setOpponentId]=useState<number|null>(null);
 const [pitcher,setPitcher]=useState<PredictionPlayer|null>(null),[pitcherHand,setPitcherHand]=useState<'ANY'|'R'|'L'>('ANY'),[homeAway,setHomeAway]=useState<'ANY'|'HOME'|'AWAY'>('ANY'),[withPlayer,setWithPlayer]=useState<PredictionPlayer|null>(null),[withoutPlayer,setWithoutPlayer]=useState<PredictionPlayer|null>(null);
 const [loading,setLoading]=useState(false),[error,setError]=useState<string|null>(null),[refreshKey,setRefreshKey]=useState(0);const requestId=useRef(0);const currentSeason=new Date().getFullYear();
 useEffect(()=>{fetchTeams().then(setTeams).catch(()=>setTeams([]))},[refreshKey]);
 useEffect(()=>{if(!player){setLogs([]);setRows([]);setError(null);return}setLoading(true);setError(null);fetchPredictionLogs(player,seasonMode).then(next=>{setLogs(next);if(!next.length)setError(`No game logs are available for ${player.name} in the selected season view.`)}).catch(e=>{setLogs([]);setRows([]);setError(e instanceof Error?e.message:'Unable to load player logs.')}).finally(()=>setLoading(false));const defs=player.group==='pitching'?PITCHER_PREDICTION_STATS:HITTER_PREDICTION_STATS;if(!defs.some(d=>d.id===statId)){setStatId(defs[0].id);setTargetIndex(0)}},[player?.id,seasonMode,refreshKey]);
 useEffect(()=>setTargetIndex(0),[statId]);
 useEffect(()=>{setPitcher(null);setPitcherHand('ANY')},[opponentId]);
 const statDefs=player?.group==='pitching'?PITCHER_PREDICTION_STATS:HITTER_PREDICTION_STATS,stat=statDefs.find(d=>d.id===statId)??statDefs[0],target=stat?.targets[targetIndex]??stat?.targets[0];
 const selectedOpponent=teams.find(t=>Number(t.id)===opponentId)??null,opponents=player?.currentTeam?.id?teams.filter(t=>Number(t.id)!==Number(player.currentTeam?.id)):teams;
 useEffect(()=>{if(!player||!stat||!target){setRows([]);return}const id=++requestId.current;const run=async()=>{setLoading(true);try{let c=[...logs];if(opponentId)c=c.filter(x=>Number(x.opponentId)===opponentId);if(homeAway!=='ANY')c=c.filter(x=>x.isHome==null||(homeAway==='HOME'?x.isHome:!x.isHome));if(windowKey==='H2H'&&!opponentId)c=[];const advanced=Boolean(pitcher||pitcherHand!=='ANY'||withPlayer||withoutPlayer);let r:any[]=advanced?await Promise.all(c.slice(0,70).map(x=>addPredictionContext(x,player,withPlayer,withoutPlayer))):c;if(pitcher)r=r.filter(x=>Number(x.opponentStarterId)===pitcher.id);if(pitcherHand!=='ANY')r=r.filter(x=>x.opponentStarterHand===pitcherHand);if(withPlayer)r=r.filter(x=>x.withPlayerActive===true);if(withoutPlayer)r=r.filter(x=>x.withoutPlayerActive===false);const limit=windowKey==='L5'?5:windowKey==='L10'?10:windowKey==='L20'?20:windowKey==='L30'?30:r.length;const next=r.slice(0,limit).map(x=>{const value=stat.value(x.stat);return {...x,value,success:succeeds(value,target)}});if(id===requestId.current)setRows(next)}catch(e){if(id===requestId.current){setRows([]);setError(e instanceof Error?e.message:'Unable to build trend.')}}finally{if(id===requestId.current)setLoading(false)}};void run()},[player?.id,logs,statId,targetIndex,windowKey,opponentId,pitcher?.id,pitcherHand,homeAway,withPlayer?.id,withoutPlayer?.id]);
 const base=useMemo(()=>logs.map(x=>({season:x.season,success:target?succeeds(stat?.value(x.stat)??0,target):false})),[logs,statId,targetIndex]);
 const seasonRate=rate(base.filter(x=>x.success).length,base.length),recent=base.slice(0,10),recentRate=rate(recent.filter(x=>x.success).length,recent.length),filtered=rate(rows.filter(x=>x.success).length,rows.length);
 const cur=base.filter(x=>x.season===currentSeason),old=base.filter(x=>x.season===2025),curRate=rate(cur.filter(x=>x.success).length,cur.length),oldRate=rate(old.filter(x=>x.success).length,old.length),weightedSeasonRate=seasonMode==='COMBINED'&&cur.length&&old.length?curRate*.7+oldRate*.3:seasonRate;
 const projection=player&&logs.length?clamp(rows.length?filtered*.45+recentRate*.30+weightedSeasonRate*.25:recentRate*.55+weightedSeasonRate*.45,.05,.95):0;
 const refresh=()=>{clearPredictionFeedCache();setRefreshKey(v=>v+1)};
 const clearFilters=()=>{setOpponentId(null);setPitcher(null);setPitcherHand('ANY');setHomeAway('ANY');setWithPlayer(null);setWithoutPlayer(null);if(windowKey==='H2H')setWindowKey('L10')};
 return {player,setPlayer,teams,logs,rows,statId,setStatId,targetIndex,setTargetIndex,windowKey,setWindowKey,seasonMode,setSeasonMode,currentSeason,opponentId,setOpponentId,pitcher,setPitcher,pitcherHand,setPitcherHand,homeAway,setHomeAway,withPlayer,setWithPlayer,withoutPlayer,setWithoutPlayer,loading,error,statDefs,stat,target,selectedOpponent,opponents,recentRate,seasonRate,projection,refresh,clearFilters};
}
